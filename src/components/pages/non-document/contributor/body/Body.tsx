import tw from "twin.macro"

// do this after subjects, collections, etc, which already have 'cards' for each document type on cms
const ContributorBody = () => {
  return <div css={[tw`border`]}>hello</div>
}

export default ContributorBody
